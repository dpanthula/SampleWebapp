general:
  artifacts:
    - "target/webapp-maven-sample-*.war"
machine: 
  java:
    version:  openjdk7
test:
  post:
      - curl -uadmin:password -T $CIRCLE_ARTIFACTS "http://william-artifactory-625959512.us-west-2.elb.amazonaws.com/artifactory/generic-local/samplewebapp"
